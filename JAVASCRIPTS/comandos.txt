/*

window.alert //  mostra um alerta com um ok.
window.confirm // mostra um alerta so que com duas opeção ok e cancelar.
window.prompt // mostra um alerta com opção de escrever algo.
 = // recebe um dado
a1 = null //null é parar deixar a variavel sem nada
var //variavel
var a1 // nome da variavel a1
var a1 = 9 //esta tendo um atribuição a variavel
int //valor inteiro
float //valor quebrado ou com ,
number //numero
string //seria um palavra
boolean //true e false
null // objeto
object // pode ser entre {} e []
array // um objeto
undefined //    undefined
function // é um função
alert("ola " + nome) // contenação

//formtação de string

    s.length
    s.toUpperCase()
    s.toLowerCase()

//formatação the Number

    n1.toFixed(2) // fixed seria para deixar ele sempre com duas casas depois da virgula
    n1.toFixed(2).replace(".",",") //o replace seria para trocar um elemento por outro
                           ^ESSE SERIA O QUE SERIA TROCADO
    
//`eu estou aprendendo ${s}` //utiliza o template string forma melhor do que a concatenação

//converção

//scring for number

    Number.(s)
    Number.parseInt(s)
    number.parseFloat(s)

//number for scring

    string(n) or n.toscrint()

//mostra na tela
    document.write = mostra na tela
    document.writeln = mostra na tela e pula um linha




typeof // mostra qual tipo do variavel

//indentificação 

//pode comecar com letra $ e _
//nao pode comecar numero
//pode usar caracteries especia no javascript

//comando para mostra valor monetario
                                style: currency é para mostra em valor monetario
    n1.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'});
        ^um string localizada por parte do mundo
















*/